ls -lh filename  *displays size of file* 
tar xf tar filename  *extract tar file*
// Find the biggest directories, so to get a sorted list containing the 20 biggest dirs.
du -m /some/path | sort -nr | head -n 20
